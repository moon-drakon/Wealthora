# Copy the value into your process environment before the first launch.
# This is an identifier, not a password or secret.
APP_OWNER_EMAIL=owner.example@northsouth.edu
# Localhost HTTP is permitted only for development; remote servers require HTTPS.
WEALTHORA_SERVER_URL=http://localhost:8080
